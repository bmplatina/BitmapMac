//
//  TokenDecoder.swift
//  Bitmap Store
//
//  Created by 재혁 on 2022/07/14.
//

import Foundation

/*
//히어로들을 집합으로 보유할 클래스 정의
class Batch: Decodable {
    var batchcomplete: String
    var query: Array<Query> = Array<Query>()
    
    init(batchcomplete:String) {
        self.batchcomplete = batchcomplete
    }
}

class Query: Decodable {
    var tokens: Array<Tokens> = Array<Tokens>()
}

//히어로 한명을 담을 클래스 정의
class Tokens: Decodable {
    var csrftoken: String
    
    init(csrftoken: String){
        self.csrftoken = csrftoken;
    }
}
*/
